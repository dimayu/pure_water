import { BrowserRouter } from "react-router-dom";

import './App.scss';

export const App = () => {
    return (
        <div className="App">
            <BrowserRouter>
            
            </BrowserRouter>
        </div>
    );
};
